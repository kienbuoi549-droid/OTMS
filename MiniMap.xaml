<!-- ============================================================
     MiniMap.xaml — OTMSAnalyzer V10 (WPF XAML)
     Popup MiniMap lò nướng (mockup MiniMap.txt -> XAML) 500x370
     Neo góc phải-dưới màn hình, luôn nổi trên (Topmost).
     V2.4: NỐI DỮ LIỆU THẬT từ 2 nguồn (không còn dữ liệu mẫu cứng):
       1. Active.db  — oven đang theo dõi của từng line: OVEN ID / FINISH /
          MAG (số kệ) / TOTAL SIP / đếm ngược / danh sách model (SP_Models)
       2. yyyymmdd.db — (mốc thời gian ca làm việc hôm nay) BATCH có finish gần
          nhất (≤ hiện tại) theo line: MM_Done* (OVEN HOÀN THÀNH GẦN NHẤT)
       V2.6: thông tin Done* lấy ĐÚNG batch có finish gần nhất trong lò
       (không cộng dồn các lô đã ra trước đó); file thật được mã hoá
       AES+GZip — NAP_DB_LICH_SU_NGAY tự nhận diện cả 2 định dạng.
     Tên 2 line (Line_Btn_*) lấy theo SETTINGS.items.
     Điều hướng chấm macOS:
       Cham_Do   = thoát ứng dụng
       Cham_Vang = ẩn (minimize) xuống taskbar
       Cham_Xanh = đóng giao diện này, mở OvenRanking.xaml
     V2.7: FIX lỗi nạp XAML "Cannot set unknown member
       'System.Windows.Controls.Grid.BorderBrush'" — thẻ <Grid> header
       gán BorderBrush/BorderThickness (2 thuộc tính KHÔNG tồn tại trên
       Grid, chỉ có trên Border). Đã bọc Grid trong 1 Border để giữ
       nguyên đường viền dưới #EEEEEE.
     ============================================================ -->
<Window xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MiniMap"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        Topmost="True" ShowInTaskbar="True" ResizeMode="NoResize" ShowActivated="True"
        Width="524" Height="300"
        FontFamily="Segoe UI" FontSize="13">

  <!-- Khung popup: bo góc 18 + đổ bóng (giống .popup của mockup) -->
  <Border x:Name="Khung_Popup" Background="White" BorderBrush="#E8EDF5" BorderThickness="1"
          CornerRadius="18" Width="500" Height="276" Margin="10" ClipToBounds="False">
    <Border.Effect>
      <DropShadowEffect Color="#0F172A" BlurRadius="30" ShadowDepth="0" Opacity="0.10"/>
    </Border.Effect>
    <Grid>
      <Grid.RowDefinitions>
        <RowDefinition Height="42"/>
        <RowDefinition Height="Auto"/>
        <RowDefinition Height="*"/>
        <RowDefinition Height="Auto"/>
      </Grid.RowDefinitions>

      <!-- ══════════ HEADER: 3 chấm + line toggle ══════════ -->
      <!-- FIX v2.7: <Grid> KHÔNG có BorderBrush/BorderThickness (2 thuộc
           tính chỉ tồn tại trên <Border>). Bọc Grid trong Border để giữ
           nguyên đường viền dưới #EEEEEE. -->
      <Border Grid.Row="0" Background="Transparent" BorderBrush="#EEEEEE" BorderThickness="0,0,0,1">
        <Grid>
          <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="14,0,0,0">
            <Grid x:Name="Cham_Do" Width="10" Height="10" Cursor="Hand" Margin="0,0,8,0"
                  Background="Transparent" ToolTip="Đóng ứng dụng">
              <Ellipse Fill="#FF5F57" StrokeThickness="0"/>
            </Grid>
            <Grid x:Name="Cham_Vang" Width="10" Height="10" Cursor="Hand" Margin="0,0,8,0"
                  Background="Transparent" ToolTip="Ẩn xuống taskbar">
              <Ellipse Fill="#FEBC2E" StrokeThickness="0"/>
            </Grid>
            <Grid x:Name="Cham_Xanh" Width="10" Height="10" Cursor="Hand" Background="Transparent"
                  ToolTip="Mở Oven Ranking">
              <Ellipse Fill="#28C840" StrokeThickness="0"/>
            </Grid>
          </StackPanel>

          <!-- Line toggle: 1 vùng bấm duy nhất, bấm bất kỳ đâu trong pill đều chuyển line -->
          <Grid x:Name="Line_Toggle" HorizontalAlignment="Center" VerticalAlignment="Center"
                Width="128" Height="30" Background="Transparent" Cursor="Hand"
                ToolTip="Bấm để chuyển line">
            <Border Background="#F1F5F9" CornerRadius="12"/>
            <Border x:Name="Line_Thumb" Background="#2563EB" CornerRadius="12"
                    Width="76" Height="24" HorizontalAlignment="Left" VerticalAlignment="Center">
              <Border.RenderTransform>
                <TranslateTransform x:Name="Line_Thumb_X" X="0"/>
              </Border.RenderTransform>
            </Border>
            <UniformGrid Columns="2">
              <TextBlock x:Name="Line_Btn_C105" Text="C10.5" FontSize="10" FontWeight="Bold"
                         Foreground="White" TextAlignment="Center" VerticalAlignment="Center" Padding="0,6"/>
              <TextBlock x:Name="Line_Btn_Hades" Text="Hades" FontSize="10" FontWeight="Bold"
                         Foreground="#6B7280" TextAlignment="Center" VerticalAlignment="Center" Padding="0,6"/>
            </UniformGrid>
          </Grid>
        </Grid>
      </Border>


      <Border Grid.Row="1" Background="White" BorderBrush="#EDF2F7" BorderThickness="1"
              CornerRadius="14" Margin="14,5,14,0" Padding="5,5">
        <Grid>
          <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>       <!-- OVEN ID: co giãn -->
            <ColumnDefinition Width="110"/>     <!-- FINISH: cố định -->
            <ColumnDefinition Width="60"/>      <!-- MAG:    cố định (đủ rộng để label "MAG" nằm giữa) -->
            <ColumnDefinition Width="100"/>     <!-- TOTAL:  cố định -->
          </Grid.ColumnDefinitions>
          <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
          </Grid.RowDefinitions>

          <!-- Hàng label (căn giữa) -->
          <TextBlock Grid.Row="0" Grid.Column="0" Text="OVEN ID" FontSize="12" FontWeight="SemiBold"
                     Foreground="#9CA3AF" HorizontalAlignment="Left"/>
          <TextBlock Grid.Row="0" Grid.Column="1" Text="FINISH" FontSize="12" FontWeight="SemiBold"
                     Foreground="#9CA3AF" HorizontalAlignment="Center"/>
          <TextBlock Grid.Row="0" Grid.Column="2" Text="MAG" FontSize="12" FontWeight="SemiBold"
                     Foreground="#9CA3AF" HorizontalAlignment="Center"/>
          <TextBlock Grid.Row="0" Grid.Column="3" Text="TOTAL" FontSize="12" FontWeight="SemiBold"
                     Foreground="#9CA3AF" HorizontalAlignment="Center"/>


          <TextBlock x:Name="MM_OvenId" Grid.Row="1" Grid.Column="0" Text="--"
                     FontSize="15" FontWeight="Bold" Foreground="#1F2937"
                     HorizontalAlignment="Left" TextTrimming="CharacterEllipsis"/>
          <TextBlock x:Name="MM_Finish" Grid.Row="1" Grid.Column="1" Text="--"
                     FontSize="15" FontWeight="Bold" Foreground="#1F2937"
                     HorizontalAlignment="Center"/>
          <TextBlock x:Name="MM_Mag" Grid.Row="1" Grid.Column="2" Text="--"
                     FontSize="15" FontWeight="Bold" Foreground="#2563EB"
                     HorizontalAlignment="Center"/>
          <TextBlock x:Name="MM_Total" Grid.Row="1" Grid.Column="3" Text="-- SIP"
                     FontSize="15" FontWeight="Bold" Foreground="#2563EB"
                     HorizontalAlignment="Center"/>
        </Grid>
      </Border>

      <!-- ══════════ DETAIL: countdown | model list ══════════ -->
      <Border Grid.Row="2" Background="White" BorderBrush="#EDF2F7" BorderThickness="1"
              CornerRadius="14" Margin="14,5,14,5" ClipToBounds="True">
        <Grid>
          <Grid.ColumnDefinitions>
            <ColumnDefinition Width="3*"/>
            <ColumnDefinition Width="7*"/>
          </Grid.ColumnDefinitions>

          <!-- Countdown còn lại -->
          <Border Grid.Column="0" BorderBrush="#EDF2F7" BorderThickness="0,0,1,0" Padding="6,0">
            <Border.Background>
              <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                <GradientStop x:Name="GS_Rem_1" Color="#FCFDFF" Offset="0"/>
                <GradientStop x:Name="GS_Rem_2" Color="#F5F9FF" Offset="1"/>
              </LinearGradientBrush>
            </Border.Background>
            <StackPanel VerticalAlignment="Center">
              <TextBlock x:Name="MM_RemHour" Text="--" FontSize="16" FontWeight="SemiBold"
                         Foreground="#6B7280" TextAlignment="Center" Margin="0,0,0,8"/>
              <TextBlock x:Name="MM_RemTime" Text="--" FontSize="18" FontWeight="Bold"
                         Foreground="#2563EB" TextAlignment="Center"/>
              <Border x:Name="MM_Badge" Background="#DC2626" CornerRadius="10" Padding="5,3"
                      HorizontalAlignment="Center" Margin="0,8,0,0" Visibility="Collapsed">
                <TextBlock x:Name="MM_Badge_Text" Text="SẮP XONG" FontSize="10" FontWeight="ExtraBold"
                           Foreground="White"/>
              </Border>
            </StackPanel>
          </Border>

          <!-- Danh sách model theo line -->
          <Border Grid.Column="1" Background="White">
            <Grid>
              <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
              </Grid.RowDefinitions>
              <Grid Grid.Row="0" Margin="16,10,16,16">
                <Grid.ColumnDefinitions>
                  <ColumnDefinition Width="*"/>
                  <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="MODEL ID" FontSize="10" FontWeight="SemiBold" Foreground="#9CA3AF"/>
                <TextBlock Grid.Column="1" Text="SIP" FontSize="10" FontWeight="SemiBold" Foreground="#9CA3AF"/>
                <!-- Đường gạch dưới header: dùng Border thay vì Grid để tránh lỗi tương tự -->
                <Border Grid.ColumnSpan="2" VerticalAlignment="Bottom"
                        BorderBrush="#F2F2F2" BorderThickness="0,0,0,1"/>
              </Grid>
              <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Padding="0,0,2,0">
                <StackPanel>
                  <!-- V2.4: danh sách model của line đang chọn — code dựng từng hàng
                       (model | tổng SIP) vào SP_Models, nguồn = Active.db (oven đang
                       theo dõi của line). Không còn dữ liệu mẫu cứng. -->
                  <StackPanel x:Name="SP_Models"/>
                </StackPanel>
              </ScrollViewer>
            </Grid>
          </Border>
        </Grid>
      </Border>

      <!-- ══════════ OVEN HOÀN THÀNH GẦN NHẤT ══════════
           V2.6: dữ liệu lấy từ yyyymmdd.db — ĐÚNG batch có finish gần nhất
           (≤ hiện tại) trong lò; MM_DoneId = "mã lò • Lô cycle",
           MM_DoneTotal = tổng SIP của riêng batch đó. -->
      <Border Grid.Row="3" CornerRadius="0,0,17,17" Padding="10,5" Cursor="Hand">
        <Border.Background>
          <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
            <GradientStop Color="#FBFEFC" Offset="0"/>
            <GradientStop Color="#F5FBF7" Offset="1"/>
          </LinearGradientBrush>
        </Border.Background>
        <StackPanel>
          <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
            <Ellipse Width="8" Height="8" Fill="#28C840" VerticalAlignment="Center">
              <Ellipse.Effect>
                <DropShadowEffect Color="#28C840" BlurRadius="0" ShadowDepth="0" Opacity="0.12"/>
              </Ellipse.Effect>
            </Ellipse>
            <TextBlock Text="OVEN HOÀN THÀNH GẦN NHẤT" FontSize="10" FontWeight="Bold"
                       Foreground="#16A34A" Margin="5,0,0,0" VerticalAlignment="Center"/>
          </StackPanel>
          <Grid>
            <Grid.ColumnDefinitions>
              <ColumnDefinition Width="*"/>
              <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0">
              <TextBlock x:Name="MM_DoneId" Text="--" FontSize="16" FontWeight="Bold" Foreground="#1F2937"/>
              <StackPanel Orientation="Horizontal" Margin="0,3,0,0">
                <TextBlock x:Name="MM_DoneTime" Text="--" FontSize="10" FontWeight="SemiBold" Foreground="#9CA3AF"/>
                <TextBlock Text=" • " FontSize="10" Foreground="#9CA3AF"/>
                <TextBlock x:Name="MM_DoneAgo" Text="Đang tải..." FontSize="10" Foreground="#16A34A"/>
              </StackPanel>
            </StackPanel>
            <StackPanel Grid.Column="1" TextBlock.TextAlignment="Right">
              <TextBlock Text="TOTAL" FontSize="11" FontWeight="SemiBold" Foreground="#9CA3AF"/>
              <TextBlock x:Name="MM_DoneTotal" Text="-- SIP" FontSize="14" FontWeight="Bold" Foreground="#16A34A"/>
            </StackPanel>
          </Grid>
        </StackPanel>
      </Border>
    </Grid>
  </Border>
</Window>
